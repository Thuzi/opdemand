contents:
  description: RabbitMQ Port Access
publish:
    contents:
        fields:
            - name: RabbitMQ Port
              description: TCP Port used to access RabbitMQ services
              template: tcp://%(public_dns_name)s:%(rabbitmq-server/port)s
              timeout: 10 
              healthyafter: 3
              interval: 30

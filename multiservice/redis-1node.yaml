name: Redis 1-node
stack: new-instance
description: Single node Redis cache including an EC2 Instance, Security Group and Key Pair
include: [ puppet, cache ]
config:
  puppet/classes: [ "opdemand::common", "opdemand::cache::redis" ]
  cache/username: admin
  cache/password: changeme123.
  cache/port: 6379
  cache/type: redis
required_services:
  components/keypair: 
  components/sg:
    ec2-sg/rules: [ "0.0.0.0/0,tcp,6379,6379" ] 

# default configuration values
config:
  # application configuration
  application/repository_url: git://github.com/opdemand/example-custom-ubuntu.git
  # puppet configuration
  puppet/classes: [ "opdemand::app::custom", "opdemand::web::nginx" ]
# publish to service consumers
publish:
  - name: Application URL
    description: URL used to access this application
    template: http://%(network/access_host)s

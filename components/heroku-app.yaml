name: Heroku (Application)
description: Heroku Application with support for automated deployment sourced from a Git repository
provider: heroku-app
modules:
  config: [ heroku-app, heroku-deploy ]
  export: [ heroku-app ]
  publish: [ heroku-app ] 
  monitor: [ refresh ]

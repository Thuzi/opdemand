name: IAM (SSL Certificate)
description: Digital server certificate for SSL/HTTPS encrypted communication
provider: iam-servercert
modules:
  config: [ service, iam-servercert ]
  export: [ iam-servercert ]
  monitor: [ refresh ]

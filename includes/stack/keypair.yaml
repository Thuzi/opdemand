include: [ base, aws, aws-kp, network ]
tiers:
  - name: Security
    description: Provides security infrastructure
    include: [ ]
    components:
      - type: aws-keypair
        include: [ aws, aws-kp ]
export_include: [ keypair ]
publish_include: [ keypair ]

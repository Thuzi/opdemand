include: [ base, aws, network, network-sg ]
tiers:
  - name: Network
    description: Provides network infrastructure
    include: [ ]
    components:
      - type: aws-sg
        include: [ aws, network, network-sg ]
export_include: [ sg ] 
publish_include: [ sg ] 

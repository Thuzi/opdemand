include: [ base, aws, network ]
tiers:
  - name: Network
    description: Provides network infrastructure
    include: [ ]
    components:
      - type: aws-sg-clean
        include: [ aws, network ]

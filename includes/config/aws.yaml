# editable service contents
contents:
  categories: [ aws ]
# service configuration
config:
  contents:
    sections:
      aws:
        name: Provider
        description: Configuration specific to AWS
        fields:
          aws/region_name:
            name: Region
            description: Name of AWS region
            type: clouds
            default: us-east-1
            sort: 0
            editable: [ none, destroyed ]
          aws/zone_name:
            name: Zone
            description: Name of AWS zone to use
            type: zone
            default:
            sort: 1
            editable: [ none, destroyed ]
          aws/key_name:
            name: Key Pair Name
            description: Key Pair Name to use
            type: text
            default:
            sort: 1
            editable: [ none, destroyed ]
          aws/key_material:
            name: Key Pair Material
            description: Key Pair Material to use
            type: text
            default:
            sort: 2
            editable: [ none, destroyed ]

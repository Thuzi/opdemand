doctype: load_balancer
impl: c2mock.network.LB
input:
  # name
  lb_name: lb-%(service/short_id)s
  # aws
  region_name: mock/region_name
  zone_name: mock/zone_name
output:
  lb/name: lb_name
  network/access_host: dns_name
  network/admin_host: dns_name